/** Semantic chip tones — maps to color system roles. */
export type ChipTone = "neutral" | "operational" | "success" | "warning" | "danger" | "info";

/** Four approved surface elevation levels. */
export type SurfaceLevel = "canvas" | "default" | "raised" | "hero";

export type KpiEmphasis = "default" | "success" | "warning" | "danger" | "info" | "operational";
