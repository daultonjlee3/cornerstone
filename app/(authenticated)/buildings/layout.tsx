export const metadata = {
  title: "Buildings | Cornerstone Tech",
  description: "Buildings at each property",
};

export default function BuildingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
