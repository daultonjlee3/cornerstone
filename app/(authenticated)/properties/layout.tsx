export const metadata = {
  title: "Properties | Cornerstone Tech",
  description: "Properties & locations",
};

export default function PropertiesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
