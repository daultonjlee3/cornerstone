export const metadata = {
  title: "Dashboard | Cornerstone Tech",
  description: "Your workspace",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
